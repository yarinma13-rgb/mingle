# Place your Google Cloud service account JSON here as:
#   google_service_account.json
#
# 1. Create a GCP project → enable Google Sheets API + Drive API
# 2. Create a service account → download JSON key
# 3. Share your CRM Google Sheet with the service account email (Editor)
# 4. Set GOOGLE_SHEET_ID in outbound/.env
#
# Never commit the JSON key.
